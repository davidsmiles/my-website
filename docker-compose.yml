version: '2'

services:

  flask:
    build: ./flask
    container_name: flask
    restart: always
    volumes:
      - "./flask:/app"
    expose:
      - 9090

  nginx:
    build: ./nginx
    container_name: nginx
    restart: always
    ports:
      - "80:80"
